package com.majuste.suportetecnico.model.enums;

//Enum é um enumerador, basicamente é um conjunto de constantes
//No banco de dados, se não for usado o toString(), ele ficaria como fosse 0,1,2,3 etc
public enum Cargo {
    CLIENTE,
    SUPORTE,
    ADMIN
}
